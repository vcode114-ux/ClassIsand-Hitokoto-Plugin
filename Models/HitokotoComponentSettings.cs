using CommunityToolkit.Mvvm.ComponentModel;

namespace HitokotoPlugin.Models;

/// <summary>
/// 单个一言组件实例的设置（每个摆放在主界面的组件实例独享）
/// 目前暂无实例级设置，作为占位方便后续扩展。
/// </summary>
public partial class HitokotoComponentSettings : ObservableObject
{
    // 示例：后续可在此添加字体大小、颜色等个性化设置
}
